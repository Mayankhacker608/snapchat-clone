# snapchat-clone

A local Snapchat-style verification demo page with a small Node.js server.

## Run

```bash
node server.js
```

Then open:

```text
http://localhost:3000
```

If port 3000 is busy, run:

```bash
PORT=3001 node server.js
```

The server stores demo submissions in `submissions.json`.
